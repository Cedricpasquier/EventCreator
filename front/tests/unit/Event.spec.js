import { mount } from '@vue/test-utils'
import Event from '@/components/Event.vue'

/*describe('Event.vue', () => {
    it('should display new event', () => {
        const wrapper = mount(Event, {
            data() {
                return {
                    event: {nomEvent: 'test', descEvent: 'test_desc', startDate: '10', endDate: '', timezone: 2}
                };
            },
        });
        wrapper.get('Button').trigger('submit')
        const titleEvent = wrapper.find("#event_title")
        expect(titleEvent.text()).toContain("Test")
    })
})*/
